/**
 * Harry Potter category — re-exports existing quiz data for the configs layer.
 * Add more category files (e.g. `marvel.ts`) and aggregate them in `index.ts`.
 */
export { HpGuessMoment1 } from "@/data/quizzes/hp-trivia";
export { hpShortQuiz } from "@/data/quizzes/hp-short-quiz";
export { hpMediumHard1 } from "@/data/quizzes/hp-medium-hard-1";
