# Copy to `.env.development.local` or `.env.production.local` to override without committing.
# Modes: `npm run dev` / `build:dev` → development; `npm run build` / `build:prod` / `preview` → production.
# See https://vitejs.dev/guide/env-and-mode.html

VITE_GA_MEASUREMENT_ID=G-JSFW6F07X6
VITE_ENABLE_ANALYTICS=true
VITE_GA_DEBUG=false
