# =============================================================================
# Template only — safe to commit. Copy to create real env files (those are gitignored).
# =============================================================================
#
#   cp .env.example .env.development    # then: npm run dev
#   cp .env.example .env.production     # then: npm run build
#
# See the copied files for full comments. Modes:
#   development  →  npm run dev, npm run build:dev
#   production   →  npm run build, npm run preview, npm run start:prod
#
# https://vitejs.dev/guide/env-and-mode.html
# =============================================================================

VITE_APP_NAME=Potter Wiki
VITE_APP_LOGO_URL=/potterwiki-logo.png

VITE_GA_MEASUREMENT_ID=G-JSFW6F07X6
VITE_ENABLE_ANALYTICS=true
VITE_GA_DEBUG=false
